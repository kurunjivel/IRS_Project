"""
IRS Phase 4 — services/ml package.

Contains:
    feature_engineering  — FeatureEngineeringService: build, validate, dataset
    dataset_generator    — DatasetGenerator: generate, save, load, validate CSV
    data_preprocessor    — DataPreprocessor: load, encode, split, return ML-ready data
"""
